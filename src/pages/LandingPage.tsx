const LandingPage = () => {
    return (
      <div className="min-h-screen">
        {/* If using exported Framer code, paste it here */}
        {/* If embedding, use an iframe like this: */}
        <iframe 
          src="https://nurtured-frictionless-653681.framer.app"
          className="w-full h-screen border-none"
          allow="autoplay"
        />
      </div>
    );
  };
  
  export default LandingPage;